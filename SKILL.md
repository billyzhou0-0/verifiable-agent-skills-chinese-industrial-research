---
name: chinese-industrial-research
description: 中国工业行业可验证调研方法论：GB标准/厂商规格/环评年报数据回源，零编造。Use when 需生产可验证的行业工程知识包。
---

# 中国工业行业调研（可验证工程知识包生产）

面向中国工业/制造业行业（食品加工、屠宰、化工、机械等）的调研工作流：产出**可验证的工程知识包**（设备型号/参数/厂房参数/公用工程 + 标准编号），每个数据点都需真实中国来源。方法已在 30+ 行业多 Worker 流水线实战验证（禽类屠宰、生猪屠宰、冷库冷链、注塑、光伏组件、水泥等）。

配套技能：`grounded-citations`——其引用台账（sources.py）必须用于全部引用编号；本技能覆盖"去哪里、怎么抓中国来源"。

## 金规则

1. **抓取时立即登记 URL 到引用台账**——绝不事后凭记忆登记。
2. **边研边写**：研究完一个行业（~20-35 个有效来源）就写它的文件，再开始下一个行业。绝不先把所有研究攒完再写（超时风险）。
3. **绝不编造**：型号/厂商/尺寸/URL 未检索到就写【待核实】。中文搜索引擎摘要经常误导，数据点优先打开原文核实。
4. 来源优先级：openstd（国家标准全文公开系统）＞ 行业标准（NY/T、SB/T）＞ 厂商官网产品页 ＞ 权威行业网站 ＞ 其他。

## 渠道配方（中国大陆网络实测）

### 标准库（三层检索，必须交叉验证）
1. **openstd.samr.gov.cn** — 国家标准全文公开系统。`std_list?p.p2=<标准号或名称>` 列表行内嵌 32 位 HCNO——一次 curl 即可提取 标准号/名称/类型/状态/发布日/实施日。**限速：关键词查询间 sleep 5-6s**（快速连查返回空列表是限速，不是"无此标准"）。**0 行结果页本身是可引用证据**（证明该标准不在 openstd）。工程建设类 GB（GB 50xxx）和部分环保 GB 不在 openstd——改引引用它们的环评报告。
2. **hbba.sacinfo.org.cn** — 全国标准信息公共服务平台。POST `stdQueryList` → JSON 记录。适合 NY/T、SB/T 和确认现行/废止。
3. **down.foodmate.net** — 食品伙伴网标准库。gb2312 编码；详情页「标准介绍」含 发布/实施日期、颁发部门、**代替关系**、适用范围。
4. **交叉验证规则**：强制性食品安全国标可能从 openstd/hbba 检索缺失——经 foodmate 或卫健委平台定位，标【待核实】并注明通道。标准会被替代（旧版废止）——始终记录代替链。

### 厂商官网（真实数据最丰富的地方）
- 先抓 `/sitemap.xml`；批量抓产品页；剥离 HTML；在稳定标记之间提取参数块。
- **规格表型厂商（数据最全）**：产品页有真实 长×宽×高/功率/产能/重量 表格。遇到这种档案，规格表和技术/工程页（产能范围）都抓。
- **营销文型厂商**：挖技术/工艺页面而非产品页——真实数字（吨电耗、调质时间、混合均匀度 CV）藏在特性段落里。
- **JS 框架官网**（所有页面共享同一 title）：curl 无效——用浏览器点参数页签提取文本。
- **新提取形态**：值藏在 `data-v="..."` HTML 属性里的表格（断言 JS 空页前先 grep 原始 HTML）；"尺寸最全"的页面（整机占地尺寸）；图片型选型 PDF → 渲染 PNG + OCR（pymupdf 渲染 + 本机 Vision OCR，繁体台版手册全规格表可提）。
- **失效分级纪律**：域名转让/出售的站仍返回 HTTP 200——必须抽查页面标题/品牌词确认归属，不能只看状态码。403/WAF 是动态失效（换 UA/时段重试 ≤10 分钟），不是永久失效。
- **搜索引擎死了时的厂商发现法**：上市公司年报/招股书胜过域名猜测（命中率 ~0）。东方财富 F10 公司概况 JSON 带联系邮箱——**邮箱域名通常即现行官网域名**。

### 上市公司文档（招股书/募集说明书/年报）
- cninfo `fulltextSearch/full` POST → JSON 含 `adjunctUrl` → 下载 PDF。**招股书是单个 PDF 里最丰富的工程来源**：房产证表（单栋车间/仓库建筑面积）、主要生产设备清单（设备名称+数量+成新率）、募投项目投资概算、工艺参数藏在行业描述里。
- PDF 提取：pymupdf。A+H 两地上市公司返回 H 股**繁体**年报——必须同时用简繁关键词 grep（`产能` 搜不到 `產能`）。

### 官方环保平台（排污许可/环评 — 公用工程/建筑参数）
- permit.mee.gov.cn（全国排污许可证管理信息平台公开端）— curl + UA 无验证码；企业排污许可信息含 行业类别、**大气/废水主要污染物种类、排放执行标准**。
- 环评报告书全本（建筑参数最丰富：占地/建筑面积/净高/柱网/排水坡度）在**省级受理公示**页附全本 PDF 直链（`downfile.jsp?filename=<md5>.pdf`）——纯 curl 可下 551 页全本。注意区分：**批复**页只有标题，**受理公示**页带全本。
- **行政审批局**（不限于生态环境厅）环评公示页常直接附件全本报告表 PDF——一份 115 页报告表可一次产出 92 台套设备一览表 + 防爆区标注 + 车间占地/建筑面积（单页最高产出）。

### 搜索引擎：别浪费时间（2026-08 实测）
中国大陆环境大部分 curl 搜索引擎被验证码/WAF/地理污染挡住。**360 搜索（www.so.com/s?q=...）可 curl** 且是唯一可靠中文发现通道：结果块 `<h3><a>` 带 **`data-mdurl="<真实URL>"`**——提取该属性绕过 JS 跳转；结果摘要常已含关键数字，先读摘要再深抓。其余情况：跳过搜索引擎直连——标准库、厂商 sitemap、B2B 平台、交易所公告 API。

## 审计方法论（"可验证"的核心）

流水线最后阶段审计每个知识包的数据真实性（只读——不改包/登记册/指南）：

1. **程序化全文本扫描**：Sources 编号连续性、未定义引用、覆盖率、`[unverified]` 残留、混合 `[数字+汉字]` 引用、【待核实】计数、表格行数。实测：30/30 包引用完整性 100%（"先建表后写作 + 独立台账"纪律生效）。
2. **抽样源验证**（每包 ≥5 URL）：并行 curl（UA + `-L` + `--compressed`）；失败分类——动态（403/WAF/间歇 000：换 UA/协议/时段重试）vs 永久（域名转让/停放：**查页面标题/品牌词，不是只看状态码**）。
3. **数值验证**（每包 2-3 个关键数）：HTML → 剥文本 grep 上下文；PDF → pymupdf + 正则。**URL 200 ≠ 数据被支持**——必须验证内容存在；**声明"页面无此值"前必须验证两种格式变体**（曾有一次"页面无此数"判定被推翻：千分位分隔符 + 实体编码破折号导致 grep 漏掉真实存在的值）。
4. **标准号验证**：foodmate 详情页 `<TITLE>` 带标准名——gb2312 解码 + 标题匹配。
5. **全量轮（用户拒绝抽样时——"每项都查，不是抽查"）**：全部跑——
   - **URL 全量**：提取全部 Sources 条目，批量 curl 3 轮（UA 轮换 / http↔https 互换 / 慢速重试），再内容检查标题/品牌。**404 ≠ 死链**：先探测站根 + sitemap 找改名/移动的页面再分类。WAF 403 = 动态（浏览器可达），不是永久。
   - **数值全量**：提取（值, [n], 行）三元组——**保留完整行**（截断会破坏多引用分组）；一行里任一 [n] 的页面支持该值即可。每个唯一 URL 抓一次（HTML 剥标签；PDF pymupdf），再按格式变体匹配：去千分位/空白、统一 ×/x/–/~/−、万/亿 缩放、吨↔万吨、kN↔吨、inch↔mm 换算。
   - **假阴性陷阱**（全踩过）：PDF 表格单元格拼接数字（"7136.649800" 破坏词边界）；JS 表格值藏 `data-v="…"` 属性 → 先对原始 HTML 重 grep `data-v=` 再宣布"不支持"；绝不在一种搜索形式后断定"值不存在"。
   - **新问题类**：范围连写 `[n]-[m]`（整轮漏检 30+ 处——加正则 `\[\d+\]\s*[-—~]\s*\[\d+\]`）；引用错页（值在另一个被引页面里）；Sources 行尾注释污染 URL。

## 验收线（分级，质量优先）

- **A 类（流水线型设备密集型行业）**：25~45 KB，来源 ≥30，数据点 ≥60，建图参数表 ≥10 行。
- **B 类（厂商官网稀少型行业）**：来源 ≥15，数据点 ≥60，建图参数 ≥10 行，≥15 KB，外加诚实声明三件套——① 头部编制说明写明失败通道，② 第八章补齐途径 ≥5 项且具体到"向哪家厂商/哪个市局"，③ 通知主进程列入补强名单。**分级线不是偷懒线**：数据点/列完整性/补齐途径不豁免。

## 用户治理要求（此类工作的硬规矩）

1. **审计必须全量逐项，不是抽查**——用户明确拒绝抽样（"我需要的不是抽查，而是每项都查"）：全部 URL、全部引用、全部数值回源。抽样审计"全部可达"的结论后来被证明是错的。
2. **评价产出先质量后速度**。
3. **补强轮只许添加信息，禁止删除已有信息**（除非审计确证为错误）——V01→V02 升级必须过 diff 审计证明零删除来源数据行；只有 B 级错误和格式残留可修改。
4. **技能/方法论文件创建前必须先与用户商谈**。

## 补强轮（按审计报告修复）

审计报告的修复指令就是任务清单（分级 + 文件/位置/问题/修复指令）；每包一个补强 Agent。工作流：① 读登记册 + 指南 + 审计报告；② 复制 V01 → V02 原地修补（V01 不动）；③ 按审计项修复，登记"问题编号 → 修复动作"；④ 审计自身假设可疑时重抓被引 URL——**页面标题裁决双占用争议，改任何一侧前两边都查**。只允许 添加/移动，绝不删除有来源数据。**覆盖率修复零编造三模式**：① 第九章说明行挂"编号 [1] 至 [N]"（[1] 为真实首条编号）；② 编制说明行挂"句中已点名的通道"编号；③ 第八章条目只在主语已有 Sources 编号时补 [n]——无对应编号的条目不补。**平反 guard**：审计报告标注"✅（平反）"的数值一律不动，更新记录注明平反即可，禁止按旧指令执行。

## 实战陷阱

- **抓取时登记**（不是写完后）＝ 抗幻觉核心；台账必须来自工具输出。
- 含 `&` 的 URL 在 shell 中需加引号，否则台账登记截断的坏 URL。
- 看起来"缺失"的标准可能在其他平台（食品安全国标）或被新版本替代——宣布缺失前查代替链。
- 厂商产品页空「产品简介」常意味着参数在图片里——标【待核实】继续走，不恋战。
- **read_file 可能把标准 UTF-8 的 .md 误判为 "Binary file"**——用终端兜底读取；不要转码或转换文件。
- **macOS BSD sed 没有 `\?`/`\/` BRE 转义**——标签/URL/文本改写用 Python（str.replace/正则）而非 BSD sed；依赖它的循环前先验证文件真的变了。
- **输出文件名严禁含 `/`、`\` 路径分隔符**——括号内并列词用"、"或"-"；落盘后 ls 确认单文件。
- **`$(grep -c X f || echo 0)` 在计数为 0 时输出两行**——比较前先取尾行。
- **表格列插入的 Python 陷阱**：6 列行是 8 个 cell（首尾空）；分隔行必须重建为 `'|' + '---|' * ncols`。
- **厂商页面可能是 GBK 编码**（其他页面是 UTF-8 时）——utf-8 解码 `errors='replace'` 会静默丢中文关键词造成假阴性；解码尝试循环 `for enc in ('gbk','utf-8')`。
- **用户台账类来源（无 URL）不要编数字编号进 Sources**——用非编号标记【用户台账】+ Sources 块末尾独立说明块。
- **更新记录行不得复述正在修复的坏模式**——写"[12]-[22]"、"[70 同线配套]"会再次触发同样的自动化 grep。用文字描述（"12 至 22 号范围连写展开"）。
- **公文文号方括号 `[2026]180号` 会被 verify 当成引用**——正文写六角括号 〔2026〕180号。任何正文含 `[数字+汉字+]` 的组合都会误报。
- **前轮"页面无此值"结论可被推翻**——接受基于前轮 absence 结论的待核实标注前先重新抓页；"标注过严"是诚实的失败方向，回源确认后可放宽。
- **用户台账数据每轮对原文重验**——子计数/证号是最易错列（前轮汇总文档也错，每轮重开原始文件）。

## 支持文件与脚本

- `scripts/audit_doc_scan.py` — 纯标准库引用完整性扫描器：Sources 编号连续性、未定义引用/覆盖率、[unverified] 残留、混合引用、**范围连写 `[n]-[m]` 检测**、【待核实】计数、表格行统计。
- `scripts/cn_std_search.py` — 纯标准库 CLI：一次搜索 openstd + hbba + foodmate。
- `scripts/openstd_std_list.py` — 纯标准库 CLI：openstd std_list 批量关键词搜索（内置 5s 限速 sleep），输出 `hcno|标准号|名称|状态|发布日|实施日` 行。
- `scripts/s360_search.py` — 纯标准库 CLI：360 搜索 + 真实 URL 提取（`python3 s360_search.py "<query>" [n]` → `标题 | data-mdurl真实URL | 摘要`）。
- `scripts/spec_table_extract.py` — 纯标准库 CLI：抓取页面（直连 curl，JS 站自动回退 r.jina.ai）并打印全部表格行 + 关键值参数行（型号/产量/功率/尺寸/重量/产能 模式）。

## 使用示例（Example usage）

**场景**：生产"某行业工程知识包"（如屠宰行业）。
**执行**：
1. openstd 检索行业标准，记录标准号/状态/代替关系
2. 厂商官网抓取设备参数（sitemap → 产品页 → 参数块提取）
3. 上市公司年报/招股书补充工厂规模数据（cninfo → PDF → pymupdf）
4. 环评报告全本提供建筑参数（省级受理公示 → 全本 PDF 直链）
5. 每条数据抓取时立即登记引用台账
6. 交付前跑审计扫描（audit_doc_scan.py）：编号连续性/未定义引用/【待核实】计数

**输出**：行业工程知识包（设备表+厂房参数+标准清单+Sources 块），全部数据可回源。
